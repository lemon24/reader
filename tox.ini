[tox]
envlist =
    py35
    py36
    pypy3
    coverage-report

[testenv]
extras =
    cli
    web-app
    enclosure-tags
    plugins
deps =
    -r{toxinidir}/test-requirements.txt
    pytest-xdist
    pytest-cov
commands = pytest --cov -n 4 -v --runslow

[testenv:coverage-report]
deps = coverage
skip_install = true
commands =
    coverage html

