[tox]
envlist =
    py35
    py36
    pypy3
    coverage-report

[testenv]
extras =
    web-app
    requests
    enclosure-tags
deps =
    -r{toxinidir}/test-requirements.txt
commands = coverage run -p -m pytest -v --runslow

[testenv:coverage-report]
deps = coverage
skip_install = true
commands =
    coverage combine
    coverage report
    coverage html

